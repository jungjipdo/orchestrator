// ============================================
// App.tsx — 이전 버전 (레거시)
// 실제 로직은 AppLayout + Dashboard로 이동됨
// 이 파일은 더 이상 main.tsx에서 사용되지 않음
// 삭제 예정 — 참조용으로만 유지
// ============================================

export default function App() {
  return null
}
